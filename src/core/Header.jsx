import {AppBar, Toolbar, Typography} from "@material-ui/core";
import React from "react";

export default () => {
    return (
        <AppBar>
            <Toolbar>
                <Typography variant="h6">
                    Common Voice Uganda
                </Typography>
            </Toolbar>
        </AppBar>
    )
}
